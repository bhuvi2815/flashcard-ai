# ===== Web framework =====
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ===== MongoDB driver (async) =====
motor==3.4.0
pymongo==4.7.2

# ===== Auth: password hashing + JWT tokens =====
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# ===== Data validation (FastAPI uses this internally) =====
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1

# ===== NLP / AI-ML (the core of this assignment) =====
# spaCy small English pipeline -- ~13 MB, CPU only, no GPU needed
spacy==3.7.5

# Hugging Face Transformers for T5 question-generation model
transformers==4.41.2
sentencepiece==0.2.0

# PyTorch CPU-only build -- ~300 MB vs ~1.9 GB for the default GPU build.
# Render free tier is CPU-only anyway so the GPU build gives zero benefit
# and burns through the 512 MB RAM limit before the server even starts.
# The --index-url flag tells pip to fetch from PyTorch's CPU-only wheel
# registry instead of PyPI's default (which ships the huge CUDA build).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cpu

# ===== Environment variables =====
python-dotenv==1.0.1
